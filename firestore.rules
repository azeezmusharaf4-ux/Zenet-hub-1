rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write on all collections and subcollections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}



